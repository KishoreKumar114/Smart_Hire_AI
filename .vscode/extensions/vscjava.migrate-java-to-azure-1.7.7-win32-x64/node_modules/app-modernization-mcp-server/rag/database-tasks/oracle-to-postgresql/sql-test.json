{
  "filePattern": {
    "shouldMatch": [
      "example.sql",
      "scripts/migration.sql",
      "database/schema.sql"
    ],
    "shouldNotMatch": [
      "mysql-schema.text",
      "mysql/schema.txt",
      "backup.md"
    ]
  },
  "codePattern": {
    "shouldMatch": [
      "VARCHAR2",
      "CREATE TABLE employees",
      "SELECT SYSDATE FROM DUAL",
      "CONNECT BY PRIOR employee_id = manager_id",
      "ROWNUM <= 10",
      "CREATE SEQUENCE EMPLOYEES_SEQ",
      "CREATE DATABASE LINK remote_db"
    ],
    "shouldNotMatch": [
      "aaa=bbb",
      "mysql-specific-keyword",
      "non-oracle-specific-pattern"
    ]
  },
  "fullTextSearch": {
    "shouldMatch": [
      "Migrate SQL scripts from Oracle to PostgreSQL",
      "Update example.sql, migration scripts, and schema.sql for Oracle to PostgreSQL migration",
      "How to migrate SQL syntax and database objects from Oracle to PostgreSQL compatibility",
      "Checklist for Oracle to PostgreSQL migration in SQL files: data types, sequences, and database links",
      "Replace VARCHAR2 with PostgreSQL-compatible data types",
      "Update CREATE TABLE, CREATE SEQUENCE, and CREATE DATABASE LINK statements for PostgreSQL",
      "Do not modify content if not related to Oracle, but always review for Oracle-specific SQL syntax"
    ],
    "shouldNotMatch": [
      "Unrelated migration context",
      "Non-database migration"
    ]
  }
}
