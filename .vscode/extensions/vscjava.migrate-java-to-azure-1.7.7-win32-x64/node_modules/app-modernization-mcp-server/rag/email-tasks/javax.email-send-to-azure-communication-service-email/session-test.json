{
  "filePattern": {
    "shouldMatch": [
      "ExampleClass.java",
      "nested/ExampleClass.java",
      "Main.java"
    ],
    "shouldNotMatch": [
      "AnotherClass.kt",
      "nested/ExampleClass.txt"
    ]
  },
  "codePattern": {
    "shouldMatch": [
      "Session session = Session.getInstance(props, authenticator);",
      "Session session = Session.getDefaultInstance(props, authenticator);",
      "MailerBuilder.usingSession(session);"
    ],
    "shouldNotMatch": [
      "String unrelated = \"UnrelatedClass\";",
      "EmailClient emailClient = new EmailClientBuilder().buildClient();"
    ]
  }
  ,
  "fullTextSearch": {
    "shouldMatch": [
      "Migrate JavaMail session API to Azure Communication Service email API",
      "JavaMail session to Azure Communication Service email",
      "Migrate Session.getInstance, Session.getDefaultInstance, MailerBuilder to Azure Communication Service email API",
      "Replace JavaMail and Simple Java Mail session logic with Azure Communication Service email API",
      "Remove session related code and replace with EmailClient using AzureDefaultCredential",
      "Endpoint and AzureDefaultCredential are required for EmailClient initialization",
      "Check EmailSendResult.getStatus() for email operation outcome"
    ],
    "shouldNotMatch": [
      "Unrelated session migration",
      "Migrate to non-Azure email service",
      "Keep using JavaMail session API without migration"
    ]
  }
}
