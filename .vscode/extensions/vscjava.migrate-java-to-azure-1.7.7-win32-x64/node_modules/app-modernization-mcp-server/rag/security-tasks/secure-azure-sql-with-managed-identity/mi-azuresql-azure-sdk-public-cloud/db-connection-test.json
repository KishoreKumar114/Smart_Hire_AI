{
  "filePattern": {
    "shouldMatch": [
      "src/MainSQL.java",
      "Sql.java"
    ],
    "shouldNotMatch": [
      "pom.xml",
      "build.gradle",
      "application.properties"
    ]
  },
  "codePattern": {
    "shouldMatch": [
      "SQLServerDataSource ds = new SQLServerDataSource();",
      "dataSource = new SQLServerDataSource();"
    ],
    "shouldNotMatch": [
      "import com.azure.messaging.servicebus.ServiceBusReceivedMessage;",
      "UnrelatedClass unrelated = new UnrelatedClass();"
    ]
  },
  "fullTextSearch": {
    "shouldMatch": [
      "How do I migrate Azure SQL authentication from connection string to managed identity in Java?",
            "What changes are needed in connection string or JDBC URL for managed identity support in Azure SQL?",
      "How to replace SQL authentication with DefaultAzureCredential in Azure SQL Java SDK?"
    ]
  }
}
