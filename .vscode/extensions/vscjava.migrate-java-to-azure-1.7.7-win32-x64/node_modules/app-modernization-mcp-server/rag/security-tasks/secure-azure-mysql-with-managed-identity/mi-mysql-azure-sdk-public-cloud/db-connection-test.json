{
  "filePattern": {
    "shouldMatch": [
      "src/DBUtil.java",
      "DatabaseBackupTool.java"
    ],
    "shouldNotMatch": [
      "pom.xml",
      "build.gradle",
      "application.properties"
    ]
  },
  "codePattern": {
    "shouldMatch": [
      "return DriverManager.getConnection(url,username, password);",
      "conexao = DriverManager.getConnection("
    ],
    "shouldNotMatch": [
      "import com.azure.messaging.servicebus.ServiceBusReceivedMessage;",
      "UnrelatedClass unrelated = new UnrelatedClass();"
    ]
  }
  ,
  "fullTextSearch": {
    "shouldMatch": [
      "Project is configured to use managed identity for Azure MySQL authentication.",
      "Configuration references Azure managed identity name for MySQL connection.",
      "Password property for MySQL is removed from configuration.",
      "JDBC connection string does not contain password segment.",
      "Passwordless authentication is enabled for MySQL connection."
    ],
    "shouldNotMatch": [
      "Project uses hardcoded password for MySQL authentication.",
      "JDBC connection string contains password segment.",
      "No managed identity name found in configuration.",
      "Password property is still present for MySQL authentication."
    ]
  }
}
